# =============================================================================
# YTRC-BackEnd — Environment Variables
# Copy this file to .env and fill in the values before deploying
# =============================================================================

# Application
NODE_ENV=production
API_PORT=2530

# Database
POSTGRES_USER=ytrc_user
POSTGRES_PASSWORD=change_this_strong_password
POSTGRES_DB=ytrc_db
DATABASE_URL=postgresql://ytrc_user:change_this_strong_password@postgres:5432/ytrc_db

# JWT
JWT_SECRET=change_this_very_long_random_secret_key_at_least_64_chars
JWT_EXPIRES_IN=7d
